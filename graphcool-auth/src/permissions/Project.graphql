query create($node_id: ID!, $user_id: ID!) {
  SomeProjectExists(filter: {
    id: $node_id
    user: {
      id: $user_id
    }
  })
}

query read($node_id: ID!, $user_id: ID!) {
  SomeProjectExists(filter: {
    id: $node_id
    user: {
      id: $user_id
    }
  })
}

query update($node_id: ID!, $user_id: ID!) {
  SomeProjectExists(filter: {
    id: $node_id
    user: {
      id: $user_id
    }
  })
}

query delete($node_id: ID!, $user_id: ID!) {
  SomeProjectExists(filter: {
    id: $node_id
    user: {
      id: $user_id
    }
  })
}
